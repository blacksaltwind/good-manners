{
  "name": "good-manners-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Good Manners - human-first UX guardrails for AI-generated interfaces",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=24"
  }
}
