import type { SelectorEvalCase } from '../../runner/types.js'

export const behaviorEvalCases: SelectorEvalCase[] = [
  { id: "loading-state", prompt: "Build a results screen that clearly shows a loading state while data is fetched.", expectedRules: { mustInclude: ['flow.loading'] }, maxContextCharacters: 4800 },
  { id: "empty-state", prompt: "Build a saved-items screen with a useful empty state when the user has no saved items.", expectedRules: { mustInclude: ['flow.empty'] }, maxContextCharacters: 4800 },
  { id: "network-retry", prompt: "Build a data screen that handles a network failure and lets the user retry.", expectedRules: { mustInclude: ['flow.network-failure', 'flow.retry'] }, maxContextCharacters: 4800 },
  { id: "validation-failure", prompt: "Build a form that validates user input and clearly handles validation failure.", expectedRules: { mustInclude: ['flow.validation-failure'] }, maxContextCharacters: 4800 },
  { id: "permission-denied", prompt: "Build an interface that requests permission and handles permission denied without a dead end.", expectedRules: { mustInclude: ['flow.permission-denied'] }, maxContextCharacters: 4800 },
  { id: "expired-session", prompt: "Build an editor that handles an expired session without losing the user's work.", expectedRules: { mustInclude: ['flow.session-expired'] }, maxContextCharacters: 4800 },
  { id: "resume-after-interruption", prompt: "Build a multi-step flow that can resume after the user is interrupted.", expectedRules: { mustInclude: ['flow.interruption-resume'] }, maxContextCharacters: 4800 },
  { id: "cancel-operation", prompt: "Build a long-running upload that the user can cancel safely.", expectedRules: { mustInclude: ['flow.cancellation'] }, maxContextCharacters: 4800 },
  { id: "partial-bulk-success", prompt: "Build a bulk import interface where some items can succeed while others fail and can be retried.", expectedRules: { mustInclude: ['flow.partial-success'] }, maxContextCharacters: 4800 },
  { id: "duplicate-action", prompt: "Build a purchase action that prevents accidental duplicate actions when the user clicks twice.", expectedRules: { mustInclude: ['flow.duplicate-action'] }, maxContextCharacters: 4800 },
  { id: "optimistic-update", prompt: "Build an optimistic save interaction that reconciles the UI if the request fails.", expectedRules: { mustInclude: ['feedback.optimistic-reconcile'] }, maxContextCharacters: 4800 },
  { id: "long-operation-feedback", prompt: "Build a long-running export screen and keep the user informed while it is processing.", expectedRules: { mustInclude: ['feedback.long-operation'] }, maxContextCharacters: 4800 },
  { id: "reversible-delete", prompt: "Build a list where removing an item is reversible with Undo.", expectedRules: { mustInclude: ['destructive.prefer-undo'], mustNotInclude: ['destructive.confirm-irreversible'] }, maxContextCharacters: 4800 },
  { id: "irreversible-delete", prompt: "Build a destructive action that permanently and irreversibly deletes all project data.", expectedRules: { mustInclude: ['destructive.confirm-irreversible'] }, maxContextCharacters: 4800 },
  { id: "preserve-form-values", prompt: "Build a form that preserves valid entered values after a recoverable submission error.", expectedRules: { mustInclude: ['form.preserve-values', 'error.preserve-input'] }, maxContextCharacters: 4800 },
  { id: "autofill-support", prompt: "Build a checkout form that supports browser autofill for user details.", expectedRules: { mustInclude: ['form.autofill'] }, maxContextCharacters: 4800 },
  { id: "allow-paste", prompt: "Build a verification form where users can paste a code into the input.", expectedRules: { mustInclude: ['form.allow-paste'] }, maxContextCharacters: 4800 },
  { id: "navigation-location", prompt: "Build settings navigation that makes the user's current location clear.", expectedRules: { mustInclude: ['navigation.current-location'] }, maxContextCharacters: 4800 },
  { id: "modal-focus", prompt: "Build a modal dialog with correct focus behavior while it is open.", expectedRules: { mustInclude: ['accessibility.modal-focus'] }, maxContextCharacters: 4800 },
  { id: "keyboard-interaction", prompt: "Build an interactive menu that works for keyboard users as well as pointer users.", expectedRules: { mustInclude: ['accessibility.keyboard'] }, maxContextCharacters: 4800 },
  { id: "responsive-reflow", prompt: "Build a responsive interface that reflows cleanly on a narrow viewport without losing core content.", expectedRules: { mustInclude: ['accessibility.reflow'] }, maxContextCharacters: 4800 },
  { id: "touch-spacing", prompt: "Build a touch interface with enough spacing between interactive controls.", expectedRules: { mustInclude: ['responsive.touch-spacing'] }, maxContextCharacters: 4800 },
  { id: "reduced-motion", prompt: "Build an animated transition that respects the user's reduced motion preference.", expectedRules: { mustInclude: ['accessibility.reduced-motion'] }, maxContextCharacters: 4800 },
  { id: "timed-interaction", prompt: "Build a timed interaction where users can extend or control the available time.", expectedRules: { mustInclude: ['accessibility.time-control'] }, maxContextCharacters: 4800 },
  { id: "image-alt-text", prompt: "Build a content page with meaningful images and appropriate alternative text.", expectedRules: { mustInclude: ['accessibility.alt-text'] }, maxContextCharacters: 4800 },
]
